VITE_RELAY_ORIGIN=https://relay.example.com
VITE_GATHER_ORIGIN=https://gather.example.com
VITE_ORBIT_ORIGIN=https://orbit.example.com

# Per-app WebMCP Origin Trial token (deployment only; keep outside the repo).
# Each origin has its own token; never reuse one origin's token for another.
# VITE_WEBMCP_ORIGIN_TRIAL_TOKEN=
